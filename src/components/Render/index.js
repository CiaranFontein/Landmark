import React from "react";
import render from "../../images/render.jpg";
import { StyledRender } from "./style";

const Render = () => {
  return <StyledRender src={render} />;
};

export default Render;
